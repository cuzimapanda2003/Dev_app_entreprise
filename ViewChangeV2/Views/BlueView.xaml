<UserControl x:Class="ViewChangeV2.Views.BlueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ViewChangeV2.Views"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="800">
    <Grid Background="CornflowerBlue" ShowGridLines="true">

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="1">
            <WrapPanel>
                <Label MinWidth="50" Margin="10">Nom :</Label>
                <TextBox MinWidth="100" Margin="10" Text="{Binding Animal.Name}"/>
            </WrapPanel>
            <WrapPanel>
                <Label MinWidth="50" Margin="10">Age :</Label>
                <TextBox MinWidth="100" Margin="10" Text="{Binding Animal.Age}"/>
            </WrapPanel>

        </StackPanel>

        <ListBox Grid.Column="0"
            ItemsSource="{Binding Animals, Mode=TwoWay}"
            DisplayMemberPath="Name"
            >
        </ListBox>

        <ListBox ItemsSource="{Binding Animals}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}" Margin="5"/>
                        <TextBlock Text="{Binding Age}" Margin="5"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>




    </Grid>
</UserControl>
